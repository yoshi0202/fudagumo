<h1 class="text-center mt-3"><%= @user.name %> </h1>
<p class="text-center">あなたのメールアドレス： <%= @user.email %></p>
<h2 class="text-center">あなたが作成したタグクラウド</h2>
<div class="container text-center">
    <div class="row">
    <% if @user.microposts.any? %>
        <% @microposts.each do |i| %>
            <div class="col-lg-3 p-3">
                <%= image_tag i.content, class: "img-fluid center-block" %>
            </div>
        <% end %>
    <% else %>
        <div class="col-lg-12 p-3">
            <p class="text-center">まだタグクラウドが作成されていません。</p>
        </div>
    <% end %>
    </div>
</div>
<%= render partial: "layouts/modal"%>
